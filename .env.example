DATABASE_URI=                         # MongoDB connection URI for the vnp-scraper database
DBMS_BACKEND_URL=                    # DBMS API base URL (re-triggers the queued property-check Lambda)
PORT=                                 # Port on which the Node.js server will run
TOKEN_PATH=                           # Local path to the saved Google OAuth token JSON file
CLIENT_ID=                            # Google OAuth client ID for API authentication
CLIENT_SECRET=                        # Google OAuth client secret for API authentication
FRONTEND_REDIRECT_URI=               # URL to redirect the user after Google OAuth completes
LOCATION_COUNTRY_CODE=               # Default country code used in scraping operations (e.g., 'US')
ENCRYPTION_KEY=                      # Encryption key used for securing sensitive data (must be 32 characters for AES-256)
BROWSERLESS_TOKEN=                   # API token for using Browserless.io (headless browser-as-a-service)
BROWSERLESS_BASE_URL=                # Browserless API base URL (default: https://production-sfo.browserless.io)
BROWSERLESS_SESSION_TTL=             # Persisted session lifetime in ms (default: 90 days)
BROWSERLESS_PROCESS_KEEP_ALIVE=      # Keep browser alive after disconnect in ms (default: same as SESSION_TTL)
BROWSERLESS_CONNECT_TIMEOUT=         # WebSocket connection timeout appended to connect URL (default: 300000)
BROWSERLESS_CONNECT_RETRIES=         # Retries when session returns 429 busy (default: 5)
BROWSERLESS_CONNECT_RETRY_DELAY_MS=  # Base delay between 429 retries in ms (default: 2000)
BROWSERLESS_USE_UNBLOCK=             # Unblock on NEW sessions only; always skipped on reused sessions (default: true)
BROWSERLESS_UNBLOCK_TIMEOUT=         # Unblock request timeout in ms (default: 300000)
BROWSERLESS_PROXY=residential        # Built-in proxy: residential or datacenter (set false/none to disable)
BROWSERLESS_PROXY_COUNTRY=us         # Proxy country ISO code (default: us; falls back to LOCATION_COUNTRY_CODE)
BROWSERLESS_PROXY_STICKY=true        # Same IP for the whole session (recommended)
BROWSERLESS_STEALTH=                 # Enable stealth on Session API (default: true)
BROWSERLESS_HEADLESS=                # Run Browserless session headless (default: false)
AWS_ACCESS_KEY_ID=                   # AWS access key ID for accessing S3
AWS_SECRET_ACCESS_KEY=               # AWS secret access key for accessing S3
AWS_REGION=                          # AWS region where the S3 bucket is hosted
AWS_S3_BUCKET=                       # Name of the AWS S3 bucket where files are uploaded
REDIRECT_URI=                        # Backend redirect URI for Google OAuth callback
OUR_CONTACT=                         # Single contact phone (for OTP). Used if OUR_CONTACTS is not set.
# OUR_CONTACTS: one entry per line. Format: phone:slot (slot = port number, used to match OTP email).
# - phone = your number for that line (used on the site for OTP).
# - slot  = port number: from email Subject "PORT 9 SMS" → use 9, or from body Receiver "9.01" → first number (9).
# Matching uses SLOT only (Sender is not used). Example: 571-245-6664:9 matches emails where slot is 9.
# Entries: 571-245-6664:9,18765551234:2 (comma-separated). One picked at random per job.
OUR_CONTACTS=
NODE_ENV=                            # Node.js environment mode: production, local
ENVIRONMENT=                         # Current scraper environment type, e.g., 'browserless', 'local'
LOCAL_BROWSER=                       # local/development only: true = system Chrome + persisted userDataDir profile
LOCAL_CHROME_PATH=                   # Optional path to Chrome binary (default: Puppeteer channel "chrome")
LOCAL_BROWSER_USER_DATA_DIR=         # Optional profile root (default: ~/.local-chrome-profiles/booking)
LOCAL_BROWSER_DEFAULT_EMAIL=         # Profile folder when no login email is available (default: default)
LOCAL_BROWSER_HEADLESS=              # Run local Chrome headless (default: false)
MAX_WORKER_THREADS=                  # Maximum number of worker threads to run concurrently
WORKER_QUEUE_SIZE=                   # Maximum number of jobs allowed in the worker queue

# Email Configuration for CAPTCHA alerts
SMTP_HOST=                           # SMTP server host (e.g., 'mailhog' for development, 'smtp.gmail.com' for production)
SMTP_PORT=                           # SMTP server port (e.g., 1025 for MailHog, 587 for Gmail)
EMAIL_USER=                          # SMTP authentication username
EMAIL_PASSWORD=                      # SMTP authentication password
EMAIL_FROM=                          # From address for outgoing emails
CAPTCHA_RECIPIENTS=                  # Comma-separated list of emails to receive CAPTCHA alerts (e.g., 'admin@company.com,dev@company.com')

# Captcha solving (OpenAI Vision)
OPENAI_API_KEY=                      # OpenAI API key used by the Vision-based captcha solver
ENABLE_OPENAI_VISION=                # Enable OpenAI Vision captcha solving (default: true)
ENABLE_BASIC_AUTO=                   # Enable the basic automatic captcha fallback (default: true)
CAPTCHA_MAX_RETRIES=                 # Captcha solve attempts before giving up (default: 3)
CAPTCHA_TIMEOUT=                     # Captcha solve timeout in ms (default: 120000)

# Google Drive / S3 token storage
S3_BUCKET_NAME=                      # S3 bucket holding the Google OAuth token
S3_TOKEN_KEY=                        # S3 object key for the Google OAuth token
AWS_DEFAULT_REGION=                  # Fallback AWS region when AWS_REGION is unset

# Misc
EMAIL_SERVICE=                       # Email provider shortcut (gmail, outlook, yahoo) instead of SMTP_HOST/PORT
PASSWORD_CHANGE_RECIPIENTS=          # Comma-separated emails notified when a Booking.com password is rotated
SWAGGER_BASE_URL=                    # Extra server URL shown in Swagger UI
BROWSER_PROFILE_PLATFORM=            # Force the anti-detection browser profile platform (default: auto-detect)
WORKER_ID=                           # Base tag for this process in worker/job assignment logs
