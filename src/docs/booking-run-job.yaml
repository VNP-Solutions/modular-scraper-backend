"/api/booking/run-job":
  post:
    tags:
      - Booking Jobs
    summary: Start booking scraping job
    description: Start a booking scraping job for the specified date range. Portfolio and property information is retrieved from the job record.
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            required:
              - jobId
              - startDate
              - endDate
            properties:
              jobId:
                type: string
                description: MongoDB ObjectId of the job to run (portfolio and property are retrieved from job record)
                example: "507f1f77bcf86cd799439011"
              startDate:
                type: string
                description: Start date for booking scraping (MM/DD/YYYY format)
                example: "01/01/2024"
              endDate:
                type: string
                description: End date for booking scraping (MM/DD/YYYY format)
                example: "01/31/2024"
    responses:
      '200':
        description: Booking scraping job completed successfully
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: integer
                  example: 200
                message:
                  type: string
                  example: Booking scraping job started successfully
                jobId:
                  type: string
                  example: "507f1f77bcf86cd799439011"
                portfolioId:
                  type: string
                  example: "507f1f77bcf86cd799439012"
                propertyId:
                  type: string
                  example: "507f1f77bcf86cd799439013"
      '400':
        description: Missing required parameters or invalid ObjectIds
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: integer
                  example: 400
                message:
                  type: string
                  example: jobId, startDate and endDate are required
      '404':
        description: Job not found
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: integer
                  example: 404
                message:
                  type: string
                  example: Job with ID 507f1f77bcf86cd799439011 not found
      '409':
        description: Job not in runnable state
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: integer
                  example: 409
                message:
                  type: string
                  example: Job is not in a runnable state. Current status: Running
      '500':
        description: Error processing booking job
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorResponse'
